/**
 * The words around the evidence chips (P7).
 *
 * The chips — their labels, which strategy shows which, how each state is
 * decided AND the sentence beneath them — live in @gil-bricks/core, because the
 * deal card, the analyser verdict and the extension panel all say them about the
 * same deal and must never disagree. This file is the app's door to that copy,
 * so it is still edited in one place and still found where config lives.
 */
export { CHIP_COPY as EVIDENCE_COPY } from '@gil-bricks/core';
